batsim.sampling
===============

.. py:module:: batsim.sampling

.. autoapi-nested-parse::

   Real-space GalSim profile sampling helpers.



Attributes
----------

.. autoapisummary::

   batsim.sampling._gsinterface


Functions
---------

.. autoapisummary::

   batsim.sampling._require_gsinterface
   batsim.sampling._prepare_transforms
   batsim.sampling._apply_prepared_transforms
   batsim.sampling._sample_galaxy_profile
   batsim.sampling._sample_galaxy_profile_on_stamp
   batsim.sampling._sample_psf_spectrum


Module Contents
---------------

.. py:data:: _gsinterface
   :value: None


.. py:function:: _require_gsinterface()

   Return the compiled C++ sampling backend, or raise a clear error.


.. py:function:: _prepare_transforms(transform_obj, xp, dtype=None)

   Move transform objects to the selected backend once per sampling call.

   Transforms that do not provide ``to_backend`` are left untouched, which
   preserves support for simple user-defined transform objects.


.. py:function:: _apply_prepared_transforms(coords, transform_obj, xp)

   Apply backend-prepared transforms to a coordinate array.

   Transforms are applied in the order supplied by the caller.


.. py:function:: _sample_galaxy_profile(gal_obj, coords, fine_scale, real_dtype)

   Sample the galaxy surface brightness using the compiled C++ backend.

   The Python renderer keeps coordinates on the selected array backend until
   this boundary, then passes NumPy coordinates to GalSim's C++ API to sample
   flux at each coordinate.


.. py:function:: _sample_galaxy_profile_on_stamp(gal_obj, stamp, transform_obj, fine_scale, real_dtype, xp, integration_order=2, profile=False)

   Sample the locally integrated galaxy profile on the fine simulation grid.

   For ``integration_order > 1``, each fine pixel is evaluated at
   Gauss-Legendre sub-pixel offsets and reduced to one integrated fine-grid
   value before returning to the FFT pipeline.


.. py:function:: _sample_psf_spectrum(psf_obj, n, fine_scale, complex_dtype)

   Sample the analytic PSF Fourier transform on an rFFT frequency grid.

   This backs ``psf_mode="kvalue"`` and returns a spectrum in the same layout
   as ``numpy.fft.rfft2``/``cupy.fft.rfft2``.


