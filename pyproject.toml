[build-system]
requires = [
  "setuptools>=61",
  "wheel",
  "pybind11>=3,<4",
  "numpy>=1.26,<2.0"
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-vv -s"

[tool.black]
line-length = 110
target-version = ["py310"]

[tool.isort]
profile = "black"
line_length = 110
